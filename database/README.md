This folder is legacy reference material from the period before the project used Supabase CLI migrations.

Do not add new schema changes here.

Moving forward, `supabase/migrations/` is the only source of truth for database schema changes.
